---
---

{
	"posts":[
	{% for item in site.posts %}
		{
			"title"		: "{{ item.title 	}}"
			"date" 		: "{{ item.date  	}}"
			"location" 	: "{{ item.location 	}}"
			"url"		: "{{ item.url }}"
		}
	{% endfor %}
	]
}